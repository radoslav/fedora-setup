---
- name: Install Mesa VA drivers for AMD graphics
  hosts: localhost
  become: yes

  tasks:
    - name: Install Mesa VA driver packages
      dnf:
        name:
          - mesa-va-drivers-freeworld
          - mesa-va-drivers-freeworld.i686
        state: present